"use client";

// Legacy compatibility wrapper. The active admin media uploader now lives
// beside the property page and uses the streaming /api/admin/media/upload route.
export { default } from "@/app/admin/properti/[id]/UploadMediaForm";
